interface:
  display_name: "Humble"
  short_description: "Repo-specific patterns and workflows for humble"
  default_prompt: "Use the humble repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true